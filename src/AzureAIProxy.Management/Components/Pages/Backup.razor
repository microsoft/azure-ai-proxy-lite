@page "/backup"
@namespace AzureAIProxy.Management.Components.Pages
<PageTitle>Backup &amp; Restore</PageTitle>

<MudStack Spacing="4">
    <Help Title="Backup & Restore" Link="https://microsoft.github.io/azure-ai-proxy-lite/" />

    <MudText Typo="Typo.body1">
        Backup and restore events and resources data. Backups are encrypted with a passphrase you provide.
    </MudText>

    <MudAlert Severity="Severity.Warning" Variant="Variant.Filled" Dense="true">
        Restoring a backup will clear all your existing data before importing the backup.
    </MudAlert>

    <MudStack Row="true" Spacing="2">
        <MudButton Variant="Variant.Filled" Color="Color.Primary"
            StartIcon="@Icons.Material.Filled.Download" Disabled="@isBusy"
            OnClick="BackupAsync">
            @(isBusy && currentOperation == "backup" ? "Encrypting..." : "Backup Data")
        </MudButton>

        <MudButton Variant="Variant.Filled" Color="Color.Secondary" HtmlTag="label"
            StartIcon="@Icons.Material.Filled.Upload" Disabled="@isBusy"
            for="restore-file-input">
            @(isBusy && currentOperation == "restore" ? "Restoring..." : "Restore Data")
        </MudButton>
        <InputFile id="restore-file-input" OnChange="RestoreBackupAsync" accept=".enc" style="display:none" />
    </MudStack>

    <MudDivider />

    <MudText Typo="Typo.h6" Color="Color.Error">Danger Zone</MudText>
    <MudText Typo="Typo.body2">
        Clear all data will permanently delete your events, resources, attendees, metrics, and related data.
    </MudText>
    <MudButton Variant="Variant.Filled" Color="Color.Error" OnClick="OpenClearDialog"
        StartIcon="@Icons.Material.Filled.DeleteForever" Disabled="@isBusy" Style="max-width: 250px">
        @(isBusy && currentOperation == "clear" ? "Clearing..." : "Clear All Data")
    </MudButton>
</MudStack>
